# Dependencies
node_modules/

# Next.js
/.next/
/out/
next-env.d.ts

# Secrets — NEVER commit
.env
.env.local
*.env

# GitHub token file (do not commit under any circumstances)
toke for github.txt
toke for github.txt:Zone.Identifier
*.Zone.Identifier

# Google Takeout exports (private personal data — never commit)
takeout-*/
**/Takeout/
*.takeout

# Legacy local data (pre-DB)
data/profile.json
data/cache/

# Logs & OS
*.log
.DS_Store
npm-debug.log*

# TS build info
*.tsbuildinfo
